spring:
  application:
    name: monobot-media-api
  profiles:
    active: local

---
spring:
  profiles: local
    data:
      mongodb:
        host: localhost
        port: 27017
        database: monobloc

---
spring:
  profiles: default
    data:
      mongodb:
        host: localhost
        port: 27017
        database: monobloc
